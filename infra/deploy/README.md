# Deploy Notes

- Deploy `apps/web` to Vercel or a Node host
- Deploy `apps/api` to Railway, Render, or Fly.io
- Apply `infra/supabase/schema.sql` and `infra/supabase/rls_policies.sql` in Supabase
- Schedule `app/workers/daily_notifications.py` with cron or platform scheduler
